# বাংলাবিদ — ধাপে ধাপে চালু করার গাইড (ক্লিক বাই ক্লিক, কোনো কোডিং লাগবে না)

টার্মিনাল বা গিট কমান্ড কিছুই লাগবে না — শুধু ব্রাউজারে মাউস দিয়ে ক্লিক করেই পুরো
ওয়েবসাইট লাইভ করা যাবে। ধাপগুলো ক্রমানুসারে করুন।

আজ এই গাইড **রেজিস্ট্রেশন সিস্টেম** লাইভ করা পর্যন্ত নিয়ে যায়। এক্সাম সেন্টার ও
স্টুডেন্ট লগইন পরের ধাপে যোগ হবে।

---

## ধাপ ১ — Google Sheet তৈরি করুন (ডেটাবেস)

1. [sheets.google.com](https://sheets.google.com) এ যান, **Blank** ক্লিক করুন।
2. উপরে বাম দিকে নাম দিন: `Banglabid Database`।
3. ব্যস, এটুকুই — কোনো ট্যাব বা হেডার নিজে লিখতে হবে না, পরের ধাপে সব অটোমেটিক হয়ে যাবে।

---

## ধাপ ২ — Google Apps Script (API সার্ভার) — একবারে সব অটো-সেটআপ

1. শিটের উপরের মেনু থেকে **Extensions → Apps Script** ক্লিক করুন। নতুন ট্যাব খুলবে।
2. এখানে যা লেখা আছে (`function myFunction() {}`) সব সিলেক্ট করে (`Ctrl+A`) মুছে ফেলুন।
3. এই প্রজেক্টের `apps-script/Code.gs` ফাইলের **সম্পূর্ণ কোড কপি করে** এখানে পেস্ট করুন।
   (এখানে **কোনো Sheet ID বসাতে হবে না** — কোডটা নিজেই বুঝে নেবে এটা কোন শিটের সাথে যুক্ত।)
4. উপরে ফ্লপি-ডিস্ক আইকনে ক্লিক করে **Save** করুন, প্রজেক্টের নাম দিন `Banglabid API`।
5. এখন উপরের দিকে একটা ড্রপডাউন মেনু দেখবেন যেখানে ফাংশনের নাম দেখায় (হয়তো `doGet` লেখা
   আছে)। সেই ড্রপডাউনে ক্লিক করে **setup** সিলেক্ট করুন।
6. ড্রপডাউনের পাশে **▶ Run** বাটনে ক্লিক করুন।
7. প্রথমবার একটা পপআপ আসবে অনুমতি চেয়ে (**Authorization required**) — **Review permissions**
   ক্লিক করুন → আপনার Google অ্যাকাউন্ট সিলেক্ট করুন → "Advanced" লেখায় ক্লিক করুন →
   "Go to Banglabid API (unsafe)" ক্লিক করুন → **Allow** ক্লিক করুন।
8. আবার **▶ Run** বাটনে ক্লিক করুন (এবার আর পপআপ আসবে না)। নিচে **Execution log**-এ
   "সেটআপ সম্পন্ন!" লেখা দেখবেন।
9. আপনার Google Sheet-এ ফিরে যান (আগের ট্যাবে) — দেখবেন **Registrations**, **Settings**,
   **Admins** নামে তিনটা ট্যাব, হেডারসহ, নিজে থেকেই তৈরি হয়ে গেছে। **Admins** ট্যাবে একটা
   ডিফল্ট লগইনও বসানো আছে:
   - Username: `admin`
   - Password: `banglabid2026`
   (সাইট লাইভ করার পর এই পাসওয়ার্ড বদলে ফেলবেন — শুধু Admins শিটের ওই ঘরে গিয়ে নতুন
   পাসওয়ার্ড টাইপ করে দিলেই হবে।)

### এখন ওয়েব API হিসেবে পাবলিশ করুন

10. Apps Script ট্যাবে ফিরে যান। উপরে ডান দিকে নীল **Deploy** বাটনে ক্লিক করুন →
    **New deployment**।
11. Deployment type-এর পাশে গিয়ার (⚙️) আইকনে ক্লিক করে **Web app** সিলেক্ট করুন।
12. সেট করুন:
    - **Execute as:** Me
    - **Who has access:** Anyone
13. **Deploy** ক্লিক করুন। আবার অনুমতি চাইলে ধাপ ৭-এর মতো Allow করুন।
14. একটা **Web app URL** দেখাবে, এরকমঃ `https://script.google.com/macros/s/xxxxxxxxxxxx/exec`
    — এই পুরো লিংকটা কপি করে কোথাও নোট করে রাখুন। এটাই আমাদের API URL।

> ⚠️ পরে `Code.gs`-এ কোনো পরিবর্তন করলে **Deploy → Manage deployments → পেন্সিল আইকন →
> Version: New version → Deploy** করতে হবে, নাহলে পরিবর্তন লাইভ সাইটে আসবে না।

---

## ধাপ ৩ — API URL ফ্রন্টএন্ড ফাইলে বসান

1. আপনার কম্পিউটারে extract করা `banglabid-website` ফোল্ডারে যান।
2. `.env.example` নামের ফাইলটা খুঁজুন। এর উপর রাইট-ক্লিক করে **Rename** করুন, নাম দিন `.env`
   (শুরুতে ডট সহ, `.example` অংশটা বাদ দিয়ে)।
3. ফাইলটা যেকোনো টেক্সট এডিটর (Notepad/TextEdit) দিয়ে খুলুন। এতে লিখুন:
   ```
   VITE_API_URL=https://script.google.com/macros/s/xxxxxxxxxxxx/exec
   ```
   (ধাপ ২.১৪-এর URL বসান, সেভ করুন।)

---

## ধাপ ৪ — GitHub অ্যাকাউন্ট তৈরি করুন

1. ব্রাউজারে যান **github.com** → উপরে ডান দিকে **Sign up**।
2. ইমেইল দিন → **Continue** → পাসওয়ার্ড দিন → **Continue** → ইউজারনেম দিন → **Continue**।
3. ভেরিফিকেশন পাজল সমাধান করুন।
4. ইমেইলে আসা কোড বসিয়ে **Verify**।
5. এরপরের প্রশ্নগুলোতে নিচে **Skip personalization** থাকলে সেটায় ক্লিক করতে পারেন।

---

## ধাপ ৫ — নতুন Repository তৈরি করুন

1. লগইন করা অবস্থায় উপরে ডান দিকে **+** → **New repository**।
2. **Repository name:** `banglabid`
3. **Public** সিলেক্ট করা আছে কিনা দেখে নিন (ডিফল্ট এটাই)।
4. **Add a README file** বক্সে ✅ **দেবেন না**।
5. সবুজ **Create repository** ক্লিক করুন।

---

## ধাপ ৬ — ফাইল আপলোড করুন (Push, ব্রাউজার দিয়ে)

1. রিপোজিটরির পেজে **uploading an existing file** নীল লিংকে ক্লিক করুন
   (না পেলে: **Add file** বাটন → **Upload files**)।
2. আপনার কম্পিউটারে `banglabid-website` ফোল্ডারটা **খুলুন** (ভিতরে ঢুকুন)।
3. ভিতরের সব ফাইল/ফোল্ডার সিলেক্ট করুন (`Ctrl+A` / Mac এ `Cmd+A`)। মনে রাখবেন —
   `.env` ফাইলটাও এই সিলেকশনে থাকবে, সমস্যা নেই।
4. সিলেক্ট করা সব ফাইল **ড্র্যাগ করে** GitHub-এর আপলোড বক্সে **ছেড়ে দিন**।
5. আপলোড শেষ হওয়া পর্যন্ত অপেক্ষা করুন।
6. নিচে স্ক্রল করে সবুজ **Commit changes** বাটনে ক্লিক করুন।

আপনার কোড এখন GitHub-এ — এটাই "push" করা।

---

## ধাপ ৭ — Netlify-তে হোস্ট করুন

1. নতুন ট্যাবে **netlify.com** → **Sign up** → **Sign up with GitHub**।
2. GitHub পপআপে **Authorize netlify** ক্লিক করুন।
3. Netlify ড্যাশবোর্ডে **Add new site → Import an existing project**।
4. **Deploy with GitHub** ক্লিক করুন, তালিকা থেকে **banglabid** বেছে নিন।
   (না দেখালে **Configure Netlify on GitHub** ক্লিক করে GitHub-এ গিয়ে অ্যাক্সেস দিন।)
5. Build settings ঠিক আছে কিনা দেখুন:
   - **Build command:** `npm run build`
   - **Publish directory:** `dist`
6. **Show advanced** ক্লিক করুন → **New variable**:
   - Key: `VITE_API_URL`
   - Value: (ধাপ ২.১৪-এর Apps Script URL — আবার বসান, .env ফাইলটা এখানে কাজ করে না,
     Netlify-কে আলাদাভাবে বলে দিতে হয়)
7. সবুজ **Deploy banglabid** ক্লিক করুন। ২-৪ মিনিট অপেক্ষা করুন।
8. উপরে **"Published"** দেখলে সাইট লাইভ। একটা লিংক দেখবেন যেমন
   `random-name-12345.netlify.app` — ক্লিক করে দেখে নিন।

### সাইটের নাম সুন্দর করা (ঐচ্ছিক)
**Site configuration → Change site name** → নতুন নাম দিন (যেমন `banglabid`) → **Save**।
এখন লিংক হবে `banglabid.netlify.app`।

---

## ধাপ ৮ — অ্যাডমিন প্যানেলে লগইন করুন

লিংক: `আপনার-সাইট.netlify.app/system-3212/admin-panel/login`
- Username: `admin`
- Password: `banglabid2026` (প্রথম লগইনের পরই Admins শিটে গিয়ে এটা বদলে ফেলুন)

সেটিংস ট্যাব থেকে কোর্স ফি, ছাড়ের ডেডলাইন, কোর্সের ছবির লিংক বসান। রেজিস্ট্রেশন ট্যাব থেকে
বিকাশ পেমেন্ট যাচাই করে কনফার্ম/রিজেক্ট করুন।

---

## পরে কোনো ফাইল বদলাতে হলে?

GitHub-এ সেই ফাইলটা আবার **Add file → Upload files** দিয়ে আপলোড করলেই Netlify নিজে থেকে
নতুন করে সাইট বিল্ড করে দেবে।

---

## সম্পূর্ণ পরীক্ষা ব্যবস্থা — কী আছে, কীভাবে কাজ করে

`/student/login`-এ স্টুডেন্ট ইমেইল+পাসওয়ার্ড দিয়ে লগইন করে `/student` পোর্টালে যাবে —
সেখান থেকে নোটিশ দেখা যাবে, সব পরীক্ষার ফলাফলের ইতিহাস দেখা যাবে, এবং চারটা পরীক্ষাই
শুরু করা যাবে (রেজিস্ট্রেশন কনফার্মড থাকতে হবে):

- **এমসিকিউ মক টেস্ট** — ৪০ মিনিট, কড়া সিকিউরিটি, তাৎক্ষণিক ফলাফল
- **অনুধাবনমূলক পরীক্ষা** (আগে "লিখিত পরীক্ষা" নামে ছিল, এখন সব জায়গায় এই নামে) — ৩০ মিনিট,
  কোনো সিকিউরিটি নেই (খাতায় লিখে ছবি তুলতে হয় বলে), ফলাফল পেন্ডিং থাকে যতক্ষণ না অ্যাডমিন
  খাতা দেখে নম্বর দেন
- **বিভাগীয় সেরা ২০ বানান প্রতিযোগিতা** — ১০টা র‍্যান্ডম ভুল বানান, একই পদ্ধতিতে মূল্যায়ন
- **লাইভ পরীক্ষা** — অ্যাডমিনের নির্ধারিত সময়ের মধ্যে যেকোনো সময় শুরু করা যায়; প্রথমে
  এমসিকিউ (৪০ মিনিট, কড়া সিকিউরিটি), শেষ হলে স্বয়ংক্রিয়ভাবে অনুধাবনমূলক অংশ (৩০ মিনিট,
  সিকিউরিটি ছাড়া) শুরু হয়

### এমসিকিউ প্রশ্ন বাছাই
প্রশ্ন আপলোডের সময় অ্যাডমিনকে ক্যাটাগরি (সাহিত্য/ব্যাকরণ) এবং ব্যাকরণ হলে উপ-ক্যাটাগরি
(বানান/অন্যান্য) বেছে দিতে হয়। **স্টুডেন্টের কাছে যে ৪০টা প্রশ্ন যায় তার উপর** ৫০% সাহিত্য,
৫০% ব্যাকরণ, আর ব্যাকরণের ভেতর ৩৫% বানান — এই অনুপাত স্বয়ংক্রিয়ভাবে বজায় থাকে (মোট
প্রশ্নব্যাংকের উপর না)। প্রতিবার এলোমেলোভাবে ভিন্ন প্রশ্ন আসে, প্রশ্নব্যাংকে যতই প্রশ্ন
জমা হোক না কেন এই নিয়ম কাজ করবে।

### অনুধাবনমূলক ও বানান প্রশ্ন তৈরি
অ্যাডমিন প্যানেলের **"অনুধাবনমূলক/বানান প্রশ্ন"** ট্যাবে উদ্দীপক লেখার জন্য রিচ-টেক্সট
এডিটর আছে (Bold/Italic/Underline, রঙ, লাইন-অ্যালাইনমেন্ট, বুলেট/নাম্বার লিস্ট, উদ্ধৃতি,
Undo/Redo) — কবিতার লাইন ব্রেক ঠিক রাখতে "⏎" বাটন ব্যবহার করুন। একটা উদ্দীপকের নিচে
যতগুলো ইচ্ছা প্রশ্ন যোগ/বাদ দেওয়া যাবে, প্রতিটার নম্বর আলাদাভাবে সেট করা যাবে। **ড্রাফট**
রাখলে স্টুডেন্ট দেখতে পাবে না, **প্রকাশ** করলেই পোর্টালে দেখা যাবে। মক/লাইভ/উভয়ের জন্য
আলাদা করে চিহ্নিত করা যাবে। মক টেস্টে প্রতিবার ৩টা এলোমেলো প্রকাশিত প্যাসেজ আসে; বানান
প্রতিযোগিতায় ১০টা এলোমেলো বানান আসে।

### খাতা মূল্যায়ন
অ্যাডমিন প্যানেলের **"খাতা মূল্যায়ন"** ট্যাবে পেন্ডিং উত্তরপত্রগুলো থাম্বনেইল আকারে
দেখা যায়। একটায় ক্লিক করলে খাতার ছবির উপর দাগানোর টুল খোলেঃ ৫ রঙের কলম (কালো/লাল/নীল/
সবুজ/হাইলাইটার), শেপ টুল (আয়তক্ষেত্র/বৃত্ত/তীর/ঠিক/ভুল চিহ্ন), টেক্সট টুল (ক্লিক করে
কিবোর্ডে লিখুন), এবং জুম ইন/আউট। মার্কিং শেষে "সেভ" করলে দাগানো ছবিটা নতুন করে Google
Drive-এ আপলোড হয় এবং নম্বর+মন্তব্যসহ স্টুডেন্টের পোর্টালে দেখা যায় — স্টুডেন্টের মূল
ছবিটা কখনো মোছা হয় না।

### লাইভ ফলাফল ও মেরিট তালিকা
অ্যাডমিন প্যানেলের **"লাইভ ফলাফল"** ট্যাবে লাইভ এমসিকিউর র‍্যাঙ্কিং টেবিল আকারে দেখা
যায়, সাথে একটা **প্রিন্ট/PDF ডাউনলোড** বাটন — এটা ব্রাউজারের প্রিন্ট ডায়ালগ খোলে, সেখান
থেকে "Save as PDF" বেছে নিলে PDF হিসেবে সেভ হয়। (আলাদা কোনো PDF-জেনারেশন লাইব্রেরি
ছাড়াই এইভাবে PDF/প্রিন্ট দুটোই পাওয়া যায়, তাই এক্সট্রা কোনো নির্ভরতা যোগ করা হয়নি।)
স্টুডেন্টরা নিজেদের পোর্টাল থেকে **"মেরিট তালিকা"** বাটনে ক্লিক করে লাইভ ও মক (সর্বোচ্চ
স্কোর) দুটোরই র‍্যাঙ্কিং দেখতে পারবে।

### সিকিউরিটি (শুধু MCQ অংশে)
- স্ক্রিন বাধ্যতামূলকভাবে ফুলস্ক্রিন হয়ে যায়
- রাইট-ক্লিক, টেক্সট সিলেকশন, কপি/কাট/পেস্ট বন্ধ থাকে
- ট্যাব পরিবর্তন করলে, উইন্ডো মিনিমাইজ করলে, বা ফুলস্ক্রিন থেকে বের হলে সতর্কতা আসে
- DevTools খোলার সাধারণ শর্টকাট (F12, Ctrl+Shift+I ইত্যাদি) এবং PrintScreen কী চাপা শনাক্ত হয়
- মোট ৩ বার সতর্কতার পর পরীক্ষা স্বয়ংক্রিয়ভাবে জমা হয়ে যায়
- একটা অপশন সিলেক্ট করলে সেটা আর বাতিল করা যায় না; একাধিক অপশন সিলেক্ট করলে উত্তর ভুল গণ্য হয়
- ফলাফলে *সব* প্রশ্নের বিশ্লেষণ দেখানো হয় (অনুত্তরিত প্রশ্নও ভুল ধরা হয়), প্রতিটার সব অপশন
  ও সঠিক উত্তর দেখানো হয়

**সততার সাথে একটা সীমাবদ্ধতা জানিয়ে রাখা জরুরি:** কোনো ওয়েবসাইটই ফোন দিয়ে স্ক্রিনের ছবি
তোলা, অন্য ডিভাইস দিয়ে স্ক্রিন রেকর্ড করা, বা OS-এর নিজস্ব স্ক্রিনশট টুল সম্পূর্ণভাবে আটকাতে
পারে না — এটা ব্রাউজার প্রযুক্তির সীমার বাইরে। অনুধাবনমূলক ও বানান পরীক্ষায় ইচ্ছাকৃতভাবে
কোনো সিকিউরিটি রাখা হয়নি (আপনার নির্দেশনা অনুযায়ী), কারণ সেখানে ছবি তোলার জন্য
ফুলস্ক্রিন/ট্যাব-লক থাকলে সমস্যা হতো।

### সরলীকরণ (সময়/খরচ সাশ্রয়ের জন্য যা একটু সহজ করা হয়েছে)
- রিচ-টেক্সট এডিটর কোনো ভারী লাইব্রেরি (যেমন React Quill) ছাড়াই ব্রাউজারের নিজস্ব
  এডিটিং কমান্ড দিয়ে বানানো হয়েছে — সব মূল ফিচার (Bold/Italic/Underline/রঙ/লিস্ট/উদ্ধৃতি/
  Undo-Redo) কাজ করে, তবে এটা Google Docs-এর মতো ভারী ফিচার-সমৃদ্ধ না।
- খাতা মূল্যায়নে Undo শুধু "সব মুছে নতুন করে শুরু" করে — ধাপে ধাপে Undo/Redo নেই।
- বানান প্রতিযোগিতার সময়সীমা নির্দিষ্ট করে বলা ছিল না, তাই ২০ মিনিট ধরে নেওয়া হয়েছে —
  `src/components/WrittenExamRunner.jsx` ফাইলের `MINUTES` অবজেক্টে বদলানো যাবে।
- লাইভ পরীক্ষার সময়সীমা একটাই শুরু-শেষ উইন্ডো (যেমন উদাহরণে দেওয়া ৮টা-১১টা) — কেউ
  উইন্ডো শেষ হওয়ার ঠিক আগে শুরু করলে মাঝপথে সময় ফুরিয়ে যেতে পারে, এটা এখনো আলাদাভাবে
  সামলানো হয়নি।

---

## ফন্ট নিয়ে নোট

নিকোশ ফন্ট মোবাইলে লোড না হওয়ার সমস্যা এড়াতে এই সাইটে **Baloo Da 2** (হেডিং) ও
**Hind Siliguri** (মূল লেখা) ব্যবহার করা হয়েছে — Google Fonts থেকে লোড হয় বলে মোবাইল ও
ডেস্কটপ দুই জায়গাতেই একই রকম দেখাবে।
